<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" type="text/css" href="/style.css">

</head>
<body>
<a href='/products/new'> Make New product</a>
<a href='/users/<%=user._id%>'> Your Profile</a>
<a href='/products'> Browse Products</a>
<a href='/auth/logout'> Logout</a>
    <h1>Products Index Page</h1>
        <ul>
        <% for(let i = 0; i < products.length; i++){ %>
            <li>
                <a href="/products/<%=products[i]._id%>"><%=products[i].title; %></a><br>
                <a href="/products/<%=products[i]._id%>/edit"><button>Edit</button></a>
                <form action="/products/<%=products[i]._id%>?_method=DELETE" method="POST">
                    <button>Remove</button>
                </form>
            </li>
        <% } %>
        </ul>
        <form action="/products/new" method="GET">
            <button>Add a New Product</button>
        </form>
</body>
</html> 